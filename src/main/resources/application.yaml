spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/todos
        username: admin
        password: admin
        driver-class-name: org.postgresql.Driver
    jpa:
        generate-ddl: true